import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MercadoLivre-OLX-Clone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
